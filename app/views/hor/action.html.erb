<body>

  <div class="jumbotron">
    <h1> <%= @execute  %></h1>
  </div>

  <% case @execute  %>  
  <% when "repartir"  %>  
      <% mc_A_valider = save_MetaclassesEnTraitement                     %>
      <h2> <%= mc_A_repartir %> </h2>
      <% executionSansErreur = system "./repartir.sh '#{mc_A_repartir}'" %>
      <%= "ERREUR D'EXECUTION" unless executionSansErreur              %>
      
  <% when "valider"    %> 
      <% mc_A_valider = save_MetaclassesEnTraitement                     %>
      <h3> <%= mc_A_valider %> </h3>
      <% executionSansErreur = system "./valider.sh '#{mc_A_valider}'"    %>
      <% msgOperationValider(executionSansErreur)                         %>
      <% effacer_DerniereFiliere($repCedulables) unless executionSansErreur %>
      <% updateMetaclassesEtat                                                 %>


  <% when "invalider"  %> 
  <% when "sample"     %> 
  <% when "variance"   %> 
  <% when "fix-ceds"   %> 
  <% when "ceduler"    %> 
  <% when "viabiliser" %> 
  <% when "compacter"  %> 
  <% when "diago"      %> 
  <% when "fix-hor"    %>
  <% when "stop"       %>
  
  <% else              %> 
      <h3> <%= "Aucune des actions programmees ne correpond a votre demande" %> </h3>
  <% end               %> 
  

  <% @blocMC = "index" unless @blocMC  %>

  <script> pageWeb(@blocMC); </script>
 
</body>