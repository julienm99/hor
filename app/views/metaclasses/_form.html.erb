<%= form_with(model: metaclass, local: true) do |form| %>
  <% if metaclass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(metaclass.errors.count, "error") %> prohibited this metaclass from being saved:</h2>

      <ul>
      <% metaclass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom, id: :metaclass_nom %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :metaclass_status %>
  </div>

  <div class="field">
    <%= form.label :niveau %>
    <%= form.text_field :niveau, id: :metaclass_niveau %>
  </div>

  <div class="field">
    <%= form.label :listeActivites %>
    <%= form.text_field :listeActivites, id: :metaclass_listeActivites %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
